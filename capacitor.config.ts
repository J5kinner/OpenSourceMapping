import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mappingthesis',
  appName: 'mapping-thesis',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
